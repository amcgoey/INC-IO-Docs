import { describe, it } from 'vitest';
import { validateHandlebarsTemplate } from './handlebars';

describe('Handlebars validation', () => {
  it.todo('should assert all Handlebar template variables exist in the DocumentType schema');
});
